name: Test Custom Action

on: [push]

jobs:
  mara_custom_action:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run custom action
        uses: ".github/actions/performance-test" # Relative path to the custom action
        with:
          name: "GitHub User"
